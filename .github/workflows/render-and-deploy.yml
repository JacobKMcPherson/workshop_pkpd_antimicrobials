name: Render and Deploy Quarto Website
  
on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

# Allow only one concurrent deployment, skipping runs queued between the run in-progress and latest queued.
# However, do NOT cancel in-progress runs as we want to allow these production deployments to complete.
concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  render-and-deploy:
    
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pages: write
      id-token: write

    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4
      with:
        lfs: true
        fetch-depth: 0

    - name: Setup Quarto
      uses: quarto-dev/quarto-actions/setup@v2
      with:
        tinytex: true

    - name: Inject Supabase secrets into survey files
      env:
        SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
        SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
      run: |
        # Set fallback values if secrets are not available (for forks/PRs)
        SUPABASE_URL_VALUE="${SUPABASE_URL:-https://YOURPROJECT.supabase.co}"
        SUPABASE_ANON_KEY_VALUE="${SUPABASE_ANON_KEY:-YOUR_PUBLIC_ANON_KEY}"
        
        # Replace placeholders in pre-workshop survey
        sed -i "s|\${SUPABASE_URL}|$SUPABASE_URL_VALUE|g" content/pre-workshop-survey.qmd
        sed -i "s|\${SUPABASE_ANON_KEY}|$SUPABASE_ANON_KEY_VALUE|g" content/pre-workshop-survey.qmd
        
        # Replace placeholders in post-workshop survey
        sed -i "s|\${SUPABASE_URL}|$SUPABASE_URL_VALUE|g" content/post-workshop-survey.qmd
        sed -i "s|\${SUPABASE_ANON_KEY}|$SUPABASE_ANON_KEY_VALUE|g" content/post-workshop-survey.qmd

    - name: Render Quarto website
      run: |
        quarto render

    - name: Render README.qmd to README.md
      run: |
        quarto render README.qmd --to md

    - name: Commit and push README.md
      run: |
        git config --local user.email "action@github.com"
        git config --local user.name "GitHub Action"
        git add README.md
        if git diff --cached --quiet; then
          echo "No changes to commit"
        else
          git commit -m "Auto-update README.md from README.qmd"
          git push
        fi

    - name: Setup Pages
      uses: actions/configure-pages@v4
      if: github.ref == 'refs/heads/main'

    - name: Upload artifact
      uses: actions/upload-pages-artifact@v3
      if: github.ref == 'refs/heads/main'
      with:
        path: '_site'

    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v4
      if: github.ref == 'refs/heads/main'
